# ============================================================
# AI 旅行规划助手 - 后端依赖
# ============================================================

# Web 框架
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
pydantic>=2.13.0

# HTTP 客户端
httpx>=0.28.0

# LLM 调用
openai>=1.30.0

# Redis 存储
redis>=5.0.0

# 长短期记忆存储
asyncpg>=0.30.0
qdrant-client>=1.12.0

# 环境变量
python-dotenv>=1.0.0

# 认证
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1

# Elasticsearch
elasticsearch>=8.12.0

# 小红书「网页扫码登录」：自己驱动浏览器，实时出二维码 + 处理二次设备安全验证
# （服务器上还需要系统库与虚拟显示：sudo bash deploy/install-xhs-login.sh）
playwright>=1.40.0

# Embedding（本地模型，可选）
# sentence-transformers>=2.2.0
